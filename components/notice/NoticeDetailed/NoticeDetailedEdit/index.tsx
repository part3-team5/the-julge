import classNames from "classnames/bind";
import styles from "./NoticeDetailedEdit.module.scss";

const cx = classNames.bind(styles);

function NoticeDetailedEdit() {
  return;
}

export default NoticeDetailedEdit;
